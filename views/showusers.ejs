<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>show users</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <style>
        table,
        th,
        td{
            border: 1px solid black;
        }
         td.password {
        color: #888;
        font-style: italic;
    }
    </style>
                    <a href="http://localhost:8000/user/add">add user</a>

    <table>
        <tr>
            <th>Id</th>
            <th>email</th>
            <th>username</th>
            <th>password</th>
        </tr>
        <%for(user of users){ %>
            <tr>
                <td><%=user.id%></td>
                <td><%=user.email%></td>
                <td><%=user.username%></td>
                <td class="password">*****************</td>
                <td>
                <form method="GET" action="/user/<%= user.id %>/edit">
                    <button>Edit username</button>
                </form>
                </td>
                <td>
                <form method="post" action="/user/<%= user.id %>/?_method=DELETE">
                    <button>delete</button>
                </form>
                </td>
            </tr>
        <%}%>
    </table>
</body>
</html>

